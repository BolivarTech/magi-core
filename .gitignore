# Rust
/target/
Cargo.lock

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
Thumbs.db
.DS_Store

# Environment
.env

# Python (fixture generator bytecode cache)
__pycache__/
*.pyc

# Claude Code
.claude/
.remember/
CLAUDE.md
CLAUDE.local.md

# SBTDD — local-only per CLAUDE.local.md §1 (not tracked).
sbtdd/
planning/
# Tooling-local artefacts.
plans/
scripts/
run-tests.py
prd.json
docs/superpowers/
# Internal dev docs — local-only (ADRs, migration guides, proposals).
# Only docs/MAGI-System-Documentation.md stays public/tracked.
dev-docs/

# Session runtime (gitignored per CLAUDE.local.md §2.2)
.claude/session-state.json

# graphify — knowledge graph IS tracked (team setup); only local-machine
# artefacts are excluded (cost log + pinned interpreter path).
graphify-out/cost.json
graphify-out/.graphify_python
