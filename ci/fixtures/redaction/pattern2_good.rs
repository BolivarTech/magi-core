// TARGET: providers/provider_url.rs
use reqwest as _;
fn redacted(&self) -> String { String::new() }
impl X {
    pub(crate) async fn send(self) -> Result<ProviderResponse, ProviderError> { todo!() }
}
