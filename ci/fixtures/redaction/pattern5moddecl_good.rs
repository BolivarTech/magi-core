// TARGET: providers/subject.rs
use reqwest as _;
#[cfg(test)]
mod tests;
impl P {
    pub fn new() -> Self {
        let m = compose_transport_message("op", &url, &chain);
    }
}
pub(crate) mod inner_tests_placeholder {}
