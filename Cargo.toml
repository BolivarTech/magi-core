[package]
name = "magi-core"
version = "2.1.0"
edition = "2024"
rust-version = "1.91"
authors = ["Julian Bolivar"]
description = "LLM-agnostic multi-perspective analysis system inspired by MAGI"
license = "MIT OR Apache-2.0"
repository = "https://github.com/BolivarTech/magi-core"
homepage = "https://github.com/BolivarTech/magi-core"
readme = "README.md"
keywords = ["llm", "multi-agent", "consensus", "analysis", "magi"]
categories = ["algorithms", "asynchronous"]

[dependencies]
thiserror = "2"
tracing = "~0.1.41"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
regex = "1"
async-trait = "0.1"
tokio = { version = "1", features = ["time", "rt", "process", "io-util", "sync"] }
reqwest = { version = "0.12", features = ["json"], optional = true }
fastrand = "~2"
unicode-normalization = "~0.1.24"
caseless = "~0.2.2"
sha2 = "0.10"

[features]
claude-api = ["dep:reqwest"]
claude-cli = []
openai-compat = ["dep:reqwest"]
# MS2: OllamaProvider reuses the OpenAI-compatible completions path and adds the
# native /api/show + /api/tags probe (window + digest). HTTP-thin, no new deps.
ollama = ["openai-compat"]
# v0.4.0: exposes RoutingMockProvider for downstream integration tests.
# Stable only within v0.4.x.
test-utils = []

[dev-dependencies]
tokio = { version = "1", features = ["macros", "rt-multi-thread", "time"] }
serial_test = "3"
reqwest = { version = "0.12", features = ["json"] }

[[example]]
name = "basic_analysis"
